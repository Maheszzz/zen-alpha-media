// CTA section is now handled by LeadMagnet.jsx (the red strip section)
// This component is kept as a simple re-export redirect to avoid breaking any legacy imports
export { default } from './LeadMagnet'
